import { Base } from './base';

export class Institution extends Base{
    name: string;
    comments: string;
    logo: string;
    shortName: string;
    cssClassContent: string;
    displayName: string;
}